export interface IQuotationItem {
  id?: string;
  vehicleId?: string;
  promotionId?: string;
  vehicleStatus?: string;
  color?: string;
  quantity?: number;
  unitPrice?: number;
  discountPrice?: number;
  totalPrice?: number;
  totalQuantity?: number;
}
